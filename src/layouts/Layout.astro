---
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import BackToTopButton from "@/components/BackToTopButton.astro";

export interface Props {
  title: string;
  author?: string;
  description?: string;
  pubDatetime?: Date;
  modDatetime?: Date | null;
  canonicalURL?: string;
  ogImage?: string;
  scrollSmooth?: boolean;
}

const {
  title,
  author = "iSamuel",
  description = "Tech reviews, opinions, and news.",
  canonicalURL = Astro.url.href,
  ogImage = "/astropaper-og.jpg",
  scrollSmooth = true,
} = Astro.props;

// Pass scrollSmooth as a JS variable for inline script
const scrollBehavior = scrollSmooth ? "smooth" : "auto";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={ogImage} />
    <link rel="canonical" href={canonicalURL} />
    <title>{title}</title>
    <script>
      document.documentElement.style.scrollBehavior = "{scrollBehavior}";
    </script>
  </head>

  <body class="bg-background text-foreground antialiased">
    <Header />

    <main class="min-h-screen mx-auto max-w-app px-4 py-8">
      <slot />
    </main>

    <BackToTopButton class="fixed bottom-4 right-4 rounded-full p-3 shadow-lg bg-background/90 z-50" />

    <Footer />
  </body>
</html>
