---
const { posts } = Astro.props;

interface PostType {
  id: string;
  data: {
    title: string;
    description?: string;
    coverImage?: string;
    coverImageAlt?: string;
    pubDatetime: Date;
    tags?: string[];
    draft?: boolean;
  };
}
---

<section class="relative w-full">
  <swiper-container
    class="mySwiper"
    slides-per-view="1"
    loop
    autoplay='{"delay": 3000, "disableOnInteraction": false}'
    navigation
    pagination='{"clickable": true}'
  >
    {posts.map((post: PostType) => (
      <swiper-slide key={post.id}>
        <a href={`/posts/${post.id}`} class="relative block">
          {post.data.coverImage && (
            <img
              src={post.data.coverImage}
              alt={post.data.coverImageAlt || post.data.title}
              class="w-full h-[300px] md:h-[380px] object-cover rounded-2xl"
              loading="eager"
            />
          )}
          <div class="absolute bottom-0 left-0 right-0 bg-black/60 text-white p-4 md:p-6 rounded-b-2xl">
            <h2 class="text-lg md:text-xl font-bold line-clamp-2">{post.data.title}</h2>
            {post.data.description && (
              <p class="mt-1 text-sm md:text-base line-clamp-2">{post.data.description}</p>
            )}
          </div>
        </a>
      </swiper-slide>
    ))}
  </swiper-container>
</section>

<style>
  .swiper-button-next,
  .swiper-button-prev {
    color: #f97316;
  }
  .swiper-pagination-bullet,
  .swiper-pagination-bullet-active {
    background: #f97316;
  }
</style>

<script is:inline type="module">
  import 'swiper/css';
  import 'swiper/css/navigation';
  import 'swiper/css/pagination';
  import { register } from 'swiper/element/bundle';

  register();
</script>
