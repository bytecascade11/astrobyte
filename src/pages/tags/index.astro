---
// src/pages/tags/index.astro

import { getCollection } from "astro:content";

import Main from "@/layouts/Main.astro";
import Layout from "@/layouts/Layout.astro";

import Tag from "@/components/Tag.astro";
import Header from "@/components/Header.astro";

import getUniqueTags from "@/utils/getUniqueTags";
import getSortedPosts from "@/utils/getSortedPosts";
import { SITE } from "@/config";

// Fetch every collection that carries tags, exclude drafts
const blogPosts = await getCollection("blog", ({ data }) => !data.draft);
const samsungPosts = await getCollection("samsung", ({ data }) => !data.draft);
const xiaomiPosts = await getCollection("xiaomi", ({ data }) => !data.draft);
const tecnoPosts = await getCollection("tecno", ({ data }) => !data.draft);
const motorolaPosts = await getCollection("motorola", ({ data }) => !data.draft);
const huaweiPosts = await getCollection("huawei", ({ data }) => !data.draft);
const honorPosts = await getCollection("honor", ({ data }) => !data.draft);
const oneplusPosts = await getCollection("oneplus", ({ data }) => !data.draft);
const pixelPosts = await getCollection("pixel", ({ data }) => !data.draft);
const vivoPosts = await getCollection("vivo", ({ data }) => !data.draft);
const freefirePosts = await getCollection("freefire", ({ data }) => !data.draft);
const oppoPosts = await getCollection("oppo", ({ data }) => !data.draft);
const applePosts = await getCollection("apple", ({ data }) => !data.draft);
const nothingPosts = await getCollection("nothing", ({ data }) => !data.draft);
const efootballPosts = await getCollection("efootball", ({ data }) => !data.draft);
const mlbbPosts = await getCollection("mlbb", ({ data }) => !data.draft);
const codmPosts = await getCollection("codm", ({ data }) => !data.draft);
const pubgmobilePosts = await getCollection("pubgmobile", ({ data }) => !data.draft);
const infinixPosts = await getCollection("infinix", ({ data }) => !data.draft);

const allPosts = [...blogPosts, ...samsungPosts, ...xiaomiPosts, ...tecnoPosts, ...motorolaPosts, ...huaweiPosts, ...honorPosts, ...oneplusPosts, ...pixelPosts, ...vivoPosts, ...freefirePosts, ...oppoPosts, ...applePosts, ...nothingPosts, ...efootballPosts, ...mlbbPosts, ...codmPosts, ...pubgmobilePosts, infinixPosts];

const sortedPosts = getSortedPosts(allPosts);
const tags = getUniqueTags(sortedPosts);
---

<Layout title={`Tags | ${SITE.title}`}>
  <Header />

  <Main pageTitle="Tags" pageDesc="All tags used across published posts.">
    {tags.length === 0 ? (
      <p class="text-center py-12">No tags found yet.</p>
    ) : (
      <ul class="tags-list grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
        {tags.map(({ tag, tagName, count }) => (
          <li>
            <Tag tag={tag} tagName={tagName} count={count} size="lg" />
          </li>
        ))}
      </ul>
    )}
  </Main>
</Layout>

<style>
  .tags-list {
    list-style: none;
    padding: 0;
    margin: 2rem 0;
  }
</style>
